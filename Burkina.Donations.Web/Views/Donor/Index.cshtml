@model IEnumerable<Burkina.Donations.Domain.Entities.Donor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DS_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DS_EMAIL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DS_PASSWORD)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DS_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DS_EMAIL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DS_PASSWORD)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DONOR }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DONOR }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DONOR })
        </td>
    </tr>
}

</table>
